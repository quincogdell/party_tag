function changePalette(label) {
  $(".img-container").addClass(label);
  $(".tweet-container").addClass(label);
  $("#canvas").addClass(label);
  $("img").addClass(label);
  $(".ui-wrapper").addClass(label);
}
